{
  "name": "smartmetrix-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix smartmetrix install && npm --prefix smartmetrix run build && node -e \"const fs=require('fs');fs.cpSync('smartmetrix/dist','dist',{recursive:true});\"",
    "vercel-build": "npm --prefix smartmetrix install && npm --prefix smartmetrix run build && node -e \"const fs=require('fs');fs.cpSync('smartmetrix/dist','dist',{recursive:true});\""
  }
}
