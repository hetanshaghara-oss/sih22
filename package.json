{
  "name": "smartmetrix-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd smartmetrix && npm install && npm run build",
    "start": "cd smartmetrix && npm run preview"
  }
}
