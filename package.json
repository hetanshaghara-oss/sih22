{
  "name": "smartmetrix-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix smartmetrix install && npm --prefix smartmetrix run build && node copy_dist.js",
    "vercel-build": "npm --prefix smartmetrix install && npm --prefix smartmetrix run build && node copy_dist.js"
  }
}